#set($subject = "$esc.html($device.name): идэвхгүй байна")
#set($lastUpdate = $dateTool.getDate())
#set($ignore = $lastUpdate.setTime($event.getLong('lastUpdate')))
#set($digest = "$device.name идэвхгүй байна ($dateTool.format('yyyy-MM-dd HH:mm:ss', $lastUpdate, $locale, $timezone)-с хойш)")
<!DOCTYPE html>
<html>
<body>
Төхөөрөмж: $esc.html($device.name)<br>
Идэвхгүй<br>
Сүүлийн шинэчлэл: $dateTool.format('yyyy-MM-dd HH:mm:ss', $lastUpdate, $locale, $timezone)<br>
Холбоос: <a href="$webUrl/event/$event.id">$webUrl/event/$event.id</a><br>
<br>
<a href="$webUrl/settings/notifications?token=$token">Мэдэгдэл цуцлах</a>
</body>
</html>
